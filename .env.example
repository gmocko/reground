# Used ONLY for the one-time manual harvest of real model output fixtures
# (examples/harvest_fixtures.py — spec RG-001 Phase 6). Set ONE of the two.
# Core library, demo, and benchmark run offline with no API key (AC8).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
